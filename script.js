document.addEventListener('DOMContentLoaded', () => {
    const lotus = document.querySelector('.lotus');
    lotus.addEventListener('mouseover', () => {
        lotus.classList.add('hover');
    });
    lotus.addEventListener('mouseout', () => {
        lotus.classList.remove('hover');
    });
});

if (condition) {
    
}